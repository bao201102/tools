/** @type {import('tailwindcss').Config} */
/**
 * Tailwind CSS v4: theme tokens live in `src/index.css` (`@theme`).
 * This file remains for tooling compatibility and explicit `content` paths.
 */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
}
